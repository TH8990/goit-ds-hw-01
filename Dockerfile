# Вказуємо базовий образ Python
FROM python:3.13.5-slim

# Встановлюємо робочу директорію всередині контейнера
WORKDIR /app

# Копіюємо файли проєкту в робочу директорію контейнера.
COPY homework.py .

# Встановлюємо команду за замовчуванням для інтерактивного запуску.
CMD ["python", "homework.py"]